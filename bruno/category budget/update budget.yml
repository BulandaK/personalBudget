info:
  name: update budget
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/v1/budgets
  body:
    type: json
    data: |-
      {
        "accountId":1,
        "category":"FOOD",
        "limitAmount":400
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
